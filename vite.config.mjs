export default {
  // Relative, so the built CSS finds its fonts whether the app is served from
  // a domain root or mounted under /photography on ckyogeshwar.com.
  base: "./",
  build: {
    outDir: "docs",
    emptyOutDir: true,
  },
};
